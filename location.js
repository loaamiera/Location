

var locScript = document.getElementById("Location");

locScript.onclick = function Location () {

    var ponderScript = locScript
    ponderScript.onclick = function pondering () {
        import java.awt
        navigator.geolocation() ;
        ponderScript.innerhtml = "Time to ponder!" ;
        
        Location.innerhtml.onclick = function (water){
            alert('Look at the water as you breathe.') ;
            Location.innerhtml.onclick = function (quote1) {
                "blach blach "
                Location.innerhtml.onlick.setText(getText()) = function (dialogue) { 
                    "How did it go?"
                } ;
            
            } ;

            } ;
        } ;


        } ;
